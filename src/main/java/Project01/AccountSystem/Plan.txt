1.主界面UI-----根据用户输入调用相应的功能
2.登录UI-----登录
3.注册UI-----注册
2.收支明细UI-----显示收支明细
3.登记收入UI-----登记收入
4.登记支出UI-----登记支出
5.收支明细功能-----显示收支明细
6.登记收入功能-----登记收入
7.登记支出功能-----登记支出


1. 用户类：存储用户的账号和密码，账号密码正确才可进入系统，存入一个csv文件
private String name;
private String password;

每一个用户在创建账户的时候会有一个自己独特的ID，按这个ID查收支明细等操作

目前的思路：需要加密，使用类似于图片加密的操作，使用bufferedOutputStream类进行处理，(^5)
在核对用户信息的时候需要解密，注意不能直接对源文件进行处理，要有一个缓冲文件,后续可以考虑性能优化吧

1. Account类：存储用户的账户信息
private long id;
private Date time;
private double balance;
public void saveMoney(double money)         //存钱
public void withdrawMoney(double money)     //取钱
public void showBalance()                   //显示余额
public void keepDetails(String detail)             //记录收支明细
public void showDetails()                        //显示收支明细
收支明细：需要写入一个csv文件，id,时间，金额，类型（收入/支出）
        "id,时间，金额，类型",写入文件可以选择FileWriter.write(String,true)方法

2.收支明细UI-----显示收支明细
将csv文件中的数据读取出来，显示在UI上，注意需要保证只输出用户的信息，而不是所有的

后续扩展：
1. 需要浏览器的页面，使用JS来处理之间的操作？
2. 将csv文件使用数据库处理。先使用csv文件进行处理，等待项目基本完成的时候边学习数据库边改进项目，需要学习：MySQL,JDBC(Java连接数据库)
3. 加密操作：使用哈希算法
4. 使用日志来保存错误信息
5. 增加对用户名和密码的验证，不能为空，不能包含特殊字符，不能包含空格，不能包含中文
6. 使用缓存来优化性能